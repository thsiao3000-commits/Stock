# 變更紀錄（Changelog）

本專案版本格式採 [語意化版本](https://semver.org/lang/zh-TW/)（MAJOR.MINOR.PATCH）。
版本號定義於 `index.html` 的 `APP_VERSION` 常數，並對應 git tag。

---

## 🔖 如何升版（維護備查）

**版號規則**（從目前版本往上加）：
- **MAJOR**（x.0.0）：重大改版、資料格式不相容（例如 localStorage 結構變更需遷移）。
- **MINOR**（1.x.0）：新增功能，且向下相容（例如多一種資產類型、新分析模式）。
- **PATCH**（1.0.x）：修 bug、文案/樣式微調，無新功能。

**升版步驟**（三處務必對齊，否則畫面、tag、紀錄會不一致）：

1. 改 `index.html` 的版本常數（唯一來源）：
   ```js
   const APP_VERSION = 'v1.2.3';
   ```
2. 更新本檔（CHANGELOG.md）新增版本區塊，並改 `README.md` 開頭的「版本：」那行。
3. commit、打 tag、推送（含 tag）：
   ```bash
   git add -A && git commit -m "Release v1.2.3"
   git tag -a v1.2.3 -m "v1.2.3 — 一句話描述"
   git push origin main
   git push origin v1.2.3
   ```

> 提醒：tag 要指向「已含新版號的 commit」，所以**先 commit 再 tag**。
> 查現有 tag：`git tag -l`；GitHub Pages 會在 push main 後自動重新部署最新版。

---

## [v1.3.0] — 2026-06-13

### 新增
- **AI 個股分析新增量能指標**：在送給 AI 的技術指標中加入 **OBV 能量潮**（近 5／20／60 日累計變化，用於判斷量價配合或背離）與 **量比**（最新量 ÷ 前 5 日均量，>1.5 放量、<0.7 量縮）。
- 報告第 3 章「技術指標解讀」明確要求 AI 結合 OBV 與量比研判量價關係。指標於程式端計算（上市／上櫃／美股一致），AI 僅負責解讀。

## [v1.2.0] — 2026-06-13

### 新增
- **台股上櫃（OTC）支援**：新增 FinMind 資料源。抓取台股時 **TWSE 為主（上市，維持原樣）→ 查無資料時自動退到 FinMind**（涵蓋上櫃，如 8358 金居），使用者無痛即可分析上櫃股，成功訊息會標示「來源 FinMind（上櫃）」。
- 「API 與參數設定」新增**選填的 FinMind Token** 欄位：免填即可使用，被限流時可填免費 token 提高額度。
- 報價更新（更新所有報價）對上櫃股同樣會自動以 FinMind 取最近收盤。

### 變更
- 上市路徑完全不變（仍走 TWSE，零風險）；FinMind 一個請求即可抓整年日線（ISO 日期，免轉民國年）。
- 抓取失敗訊息簡化為「資料不足／代號有誤」（上櫃已支援，不再提示「僅支援上市」）。

## [v1.1.1] — 2026-06-13

### 修正
- 抓取台股失敗時改用更精準的訊息：查無上市資料時提示「**可能為上櫃（OTC）股票，本工具僅支援上市**」並附上 `twse_realtime.py（otc_代號）` 的查價方式；資料不足時顯示實際抓到的天數。
- 修正抓取失敗後仍**殘留上一檔股票的價格／持倉與圖表**的問題：失敗時清空圖表資訊、隱藏走勢圖、並停用「產生個股 AI 分析」鈕（避免對舊資料分析）。

## [v1.1.0] — 2026-06-13

### 新增
- **AI 作業中動畫**：產生報告期間，狀態列顯示轉圈動畫＋經過秒數＋階段文字（**思考中 → 撰寫中**）。秒數持續跳動，讓使用者確知 AI 仍在運作而非當機——尤其 reasoning 模型在輸出第一個字前可能先靜默思考數十秒。退回 `chat/completions` 時動畫照常並標示「改用相容介面」。

### 文件
- README 補充「作業中動畫」與「報告可保存」功能說明，FAQ「AI 分析很慢」加註動畫判讀方式。

## [v1.0.0] — 2026-06-13

首個正式版本。個人投資組合追蹤 + AI 分析工具（單檔純前端）。

### 功能
- **三種資產**：台股（TWSE 收盤）、美股（Twelve Data）、海外公司債（手動 Mark）。
- **加權平均成本法**損益：未實現／已實現／股息息收／總損益，全部換算新台幣加總；各市場小計與占比。
- **逐筆交易**：買進／賣出／現金股息補登；台股手續費與證交稅自動試算。
- **債券**：自動推算配息行事曆、已收息（扣前手息）、應計利息；到期以面額計。
- **走勢圖**：近一年日線 + 我的均價虛線 + 買賣點標記 + 懸停乖離。
- **AI 個股分析**：52 週週線 + 60 日日線 + 技術指標 + 大盤對照 + 我的持倉，串流即時顯示。
- **AI 投資組合總評**：配置／集中度／幣別風險／再平衡建議。
- **報告**：產生時間戳、AI 模型、工具版本；可複製、下載為 `.md`。
- **資料**：全部存於瀏覽器 localStorage，不上傳伺服器。
- **匯率**：open.er-api.com 自動抓取（12 小時快取）或手動指定。

### 部署
- 可本機開啟，或部署至 GitHub Pages（`index.html` 為入口）。
